<% if logged_in? %>
  <%= render 'shared/content_title', title: "New Comment!" %>
<% end %>

<%= render 'shared/error', errors: @comment.errors %>

<%= render 'form', post: @post, comment: @comment %>
